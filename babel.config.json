{
  "presets": [
    "@babel/preset-typescript",
    [
      "@babel/env",
      {
        "targets": "defaults"
      }
    ],
    [
      "@babel/preset-react",
      {
        "runtime": "automatic"
      }
    ]
  ]
}
